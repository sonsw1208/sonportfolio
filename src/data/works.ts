import content from "./content.json";
import type { WorksSection } from "./types";

// Nguồn sự thật thật sự là content.json — xem ghi chú trong profile.ts.
export const works: WorksSection = content.works;
